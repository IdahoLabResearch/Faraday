# Postgres

Faraday uses Postgres for data warehousing and account management. It should use DeepLynx in the future

In this `/server` directory, create a file named `.env.local` and write contents as instructed in the `.env.local.example`
