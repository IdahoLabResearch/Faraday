services:
  faraday-app-service:
    build:
      context: ./app
      dockerfile: Dockerfiles/Dockerfile.prod.local
    ports:
      - "5173:5173"
  faraday-server-service:
    build:
      context: ./server
      dockerfile: Dockerfiles/Dockerfile.prod.local
    env_file:
      - ./server/server/.env.local
    entrypoint: ["bash", "./entrypoint.prod.sh"]
    ports:
      - "8000:8000"
    depends_on:
      - postgres
  faraday-db-service:
    build:
      context: ./database
      dockerfile: Dockerfiles/Dockerfile.prod.local
    env_file:
      - ./database/.env.local
    ports:
      - "5432:5432"
  faraday-pgadmin-service:
    image: dpage/pgadmin4
    env_file:
      - ./database/.env.local
    ports:
      - "5050:80"
